<Window x:Class="Oops.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Oops Settings"
        Height="420"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hotkey"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <ComboBox x:Name="HotkeyCombo"
                  Grid.Row="1"
                  Margin="0,0,0,16"
                  SelectionChanged="HotkeyCombo_SelectionChanged">
            <ComboBoxItem Content="Ctrl + I" Tag="CtrlI"/>
            <ComboBoxItem Content="Ctrl + Shift + I" Tag="CtrlShiftI"/>
            <ComboBoxItem Content="Alt + I" Tag="AltI"/>
        </ComboBox>

        <CheckBox x:Name="StartupCheck"
                  Grid.Row="2"
                  Content="Start with Windows"
                  Margin="0,0,0,10"/>

        <CheckBox x:Name="SoundCheck"
                  Grid.Row="3"
                  Content="Play sound after conversion"
                  Margin="0,0,0,10"/>

        <CheckBox x:Name="NotificationsCheck"
                  Grid.Row="4"
                  Content="Show &quot;Converted&quot; notification"
                  Margin="0,0,0,16"/>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="Save_Click"/>
            <Button Content="Close"
                    Width="90"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
